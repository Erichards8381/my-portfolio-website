import { FullScreenImageDirective } from './full-screen-image.directive';

describe('FullScreenImageDirective', () => {
  it('should create an instance', () => {
    const directive = new FullScreenImageDirective();
    expect(directive).toBeTruthy();
  });
});
